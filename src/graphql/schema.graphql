# import * from './prisma.graphql'
type Mutation {
  createLabel(
    name: String
    description: String
    slug: String
    extends: ExtendsUpdateManyInput
  ): Label!
}
type Query {
  label(where: LabelWhereUniqueInput): Label
  labels: [Label]!
}
